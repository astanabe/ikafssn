# Build output
build/
CMakeFiles/

# NCBI C++ Toolkit (source, tarball, and install)
ncbi-cxx-toolkit/
ncbi-cxx-toolkit-public-release-*
ncbi-toolkit/

# Parasail (source, tarball, and install)
/parasail-*
parasail/

# htslib (source, tarball, and install)
htslib-*
htslib/

# Drogon (source, tarball, and install)
drogon-*
drogon/

# Homebrew tap (separate repository)
homebrew-ikafssn/

# Conda channel (separate repository)
conda-ikafssn/

# APT channel (separate repository)
deb-ikafssn/

# DNF channel (separate repository)
rpm-ikafssn/

# Claude Code conversation logs
claude*.txt

# Local SIMD vectorization plan (working notes, not part of the repo)
vectorize_plan.md

# Local planning / design notes (working notes, not part of the repo)
improveplan.md
crosstemplate.md
eliasfano.md
faster_spaced_seed.md

# Local benchmark harness + its binary copy and output dirs (working tools, not part of the repo)
/benchmark.sh
/ikafssnsearch
/bench_*/

# Local development BLAST DBs (not part of the repo)
# SSU_eukaryote_rRNA is intentionally tracked as the shared test fixture.
db/tsa_nt.*
db/taxdb.*
db/taxonomy4blast.sqlite3
